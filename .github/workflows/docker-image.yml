name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Build the Docker image
      run: docker build -t test12 .

    - name: Tag the Docker image
      run: docker tag sha256:6c16499fe6477520124de9ce535ebcbe456e5b9458cef145269eb687760a2386 shaaantanu/test

    - name: Docker login
      run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}

    - name: Docker push
      run: docker push shaaaantanu/test
